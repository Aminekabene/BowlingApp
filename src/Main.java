public class Main {
    public static void main(String[] args) {
        
        // Here is a simulation of a bowling game with a perfect score.
        Game game = new Game();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();

    }
}
