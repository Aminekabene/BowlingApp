public class Main {
    public static void main(String[] args) {

        Game game = new Game();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);
        game.score();
        game.roll(10);

        game.score();

    }
}